# Raspberry-pi-AWS-Web
A system that contains a python app ran on a raspberry pi qemu emulator, data generated from this app is streamed to AWS using MQTT, then data stored in dynamo DB, a flask website the streams thesame mqtt topic to display live data on a web dashbaord
